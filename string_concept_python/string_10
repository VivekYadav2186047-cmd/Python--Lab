# que = Take a user input string and check if it is a palindrome (same forwards and backwards).

string1 = input("enter the name or string to check : ")
if(string1 == string1[::-1]):
    print("the string is a palindrome")

else:
    print("the string is not a palindrome")
